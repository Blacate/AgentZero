# Agent Zero

从零开始实现一个 Agent 框架，用于学习和理解 Agent 的核心原理与架构设计。

本项目是一个教学性质的项目，通过逐步构建的方式，探索 Agent 框架的基础能力。

## Setup

Install the dependencies:

```bash
pnpm install
```

## Get started

Build the library:

```bash
pnpm run build
```

Build the library in watch mode:

```bash
pnpm run dev
```
