# Plan: 实现 Tool 抽象类与具体 Tools

## Chapter 1: Core Changes & Clarification

### 1.1 Core Changes Review

| 模块 | 变更类型 | 说明 | Review |
|---|---|---|---|
| `src/agent-loop.ts` | 修改 | `Tool` interface → `abstract class Tool`，`AgentLoop` 类型引用同步更新 | - |
| `src/model.ts` | 无变更 | 保持现有 `ToolDefinition` 等类型不变 | - |
| `src/tools/tool.ts` | 新增 | abstract Tool 基类：自动组装 definition、公共参数校验、错误捕获 | - |
| `src/tools/write.ts` | 新增 | Write Tool：覆盖写入，自动创建父目录 | - |
| `src/tools/read.ts` | 新增 | Read Tool：按行读取，支持 offset/limit 切片 | - |
| `src/tools/glob.ts` | 新增 | Glob Tool：基于 `node:fs/promises.glob` | - |
| `src/tools/grep.ts` | 新增 | Grep Tool：调用系统 `rg` | - |
| `src/tools/web-fetch.ts` | 新增 | WebFetch Tool：fetch + 大小截断 | - |
| `src/tools/index.ts` | 新增 | 统一导出所有 tool | - |
| `tests/*.test.ts` | 新增 | 各 tool 的单元测试 | - |

**行为预期变更**：AgentLoop 不再接受 plain object 作为 Tool，只接受 `Tool` abstract class 的实例。所有具体 tool 继承基类，子类只需实现 `name`、`description`、`parametersSchema` 和 `run()`。

### 1.2 Clarification Questions

| # | 问题 | 选项 | 用户选择 | Review Status |
|---|---|---|---|---|
| 1 | `Tool` 用 abstract class 还是保留 interface？ | A. 替换为 abstract class<br>B. 保留 interface<br>C. 两者并存 | **A** | ✅ |
| 2 | abstract class 提供哪些公共能力？ | A. 极简壳子<br>B. 自动组装 definition<br>C. 自动组装 + 参数校验 + 错误捕获<br>D. 泛型类型安全 | **C** | ✅ |
| 3 | 文件组织方式？ | A. 全部塞进一个文件<br>B. 每个 tool 一个文件 + index.ts<br>C. 按类别分组 | **B** | ✅ |
| 4 | Write Tool 行为？ | A. 极简覆盖写入<br>B. 覆盖写入 + 自动创建目录<br>C. 支持追加模式 | **B** | ✅ |
| 5 | Read Tool 是否支持分段读取？ | A. 极简读取<br>B. 完整读取 + 大小截断<br>C. 支持分段读取 | **C** | ✅ |
| 6 | Read 的 offset/limit 单位？ | A. 按字节<br>B. 按行<br>C. 混合 | **B** | ✅ |
| 7 | Glob Tool 实现方式？ | A. Node 内置 `fs.glob`<br>B. 自己递归实现<br>C. 引入 glob 库 | **A** | ✅ |
| 8 | Grep Tool 实现方式？ | A. 纯 JS 实现<br>B. 调用系统 `rg`<br>C. 混合 | **B** | ✅ |
| 9 | WebFetch 做到什么程度？ | A. 原始 fetch<br>B. fetch + 大小截断<br>C. 提取正文 | **B** | ✅ |

### 1.3 需求演进与 Review 历史

- **原始需求**：实现一个 abstract Tool class，再创建 Write、Read、WebFetch、Grep、Glob 几种 tool，需要先写 plan。
- **Review 阶段提出的问题**：
  1. 现有 `Tool` interface 与新的 abstract class 的关系如何处理？
  2. abstract class 应提供哪些公共能力？
  3. tool 的文件组织方式？
  4. Write Tool 是否需要自动创建目录、支持追加模式？
  5. Read Tool 是否需要支持分段读取？
  6. Read 的 offset/limit 按行还是按字节？
  7. Glob 用内置 fs 还是外部库？
  8. Grep 用纯 JS 还是系统 rg？
  9. WebFetch 是否需要大小截断或正文提取？

---

> **Chapter 1 已确认**。

---

## Chapter 2: Detailed Technical Plan

### 2.1 Design Overview

```
┌─────────────────────────────────────────────────────────┐
│  AgentLoop                                               │
│   - tools: Tool[]   ← Tool class 实例                   │
│   - invoke model → 拿到 tool_calls                      │
│   - tool.execute(args) → 返回 string                    │
└─────────────────────────────────────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ Tool     │    │ Write    │    │ Read     │
    │ (class)  │    │ Tool     │    │ Tool     │
    │          │    │ instance │    │ instance │
    └──────────┘    └──────────┘    └──────────┘
         │
    ┌────┴────┬──────────┐
    ▼         ▼          ▼
┌───────┐ ┌───────┐ ┌──────────┐
│ Glob  │ │ Grep  │ │ WebFetch │
│ Tool  │ │ Tool  │ │ Tool     │
└───────┘ └───────┘ └──────────┘
```

`Tool` 是一个 **concrete class**，通过 `new Tool({})` 实例化。每个 tool 只需要提供配置对象，不需要写子类。

基类负责三件事：
1. **自动组装 `ToolDefinition`** —— 通过 `zod-to-json-schema` 把 zod schema 转成 JSON Schema
2. **运行时参数校验** —— 通过 `schema.parse(args)` 自动校验并推导类型
3. **统一错误捕获** —— `run()` 抛出的异常自动转成错误字符串，不阻断 AgentLoop

### 2.2 File Changes

| 路径 | 操作 | 说明 |
|---|---|---|
| `src/tools/tool.ts` | 新增 | Tool concrete class（zod + zod-to-json-schema） |
| `src/tools/write.ts` | 新增 | `new Tool({...})` 实例化 write tool |
| `src/tools/read.ts` | 新增 | `new Tool({...})` 实例化 read tool |
| `src/tools/glob.ts` | 新增 | `new Tool({...})` 实例化 glob tool |
| `src/tools/grep.ts` | 新增 | `new Tool({...})` 实例化 grep tool |
| `src/tools/web-fetch.ts` | 新增 | `new Tool({...})` 实例化 web-fetch tool |
| `src/tools/index.ts` | 新增 | 统一导出所有 tool 实例 |
| `package.json` | 修改 | 新增 `zod` 和 `zod-to-json-schema` 依赖 |
| `src/agent-loop.ts` | 修改 | 删除 `interface Tool`，改为从 `tools/tool.js` import Tool class |
| `src/index.ts` | 修改 | 追加导出所有 tool |
| `tests/tool.test.ts` | 新增 | Tool class 行为测试 |
| `tests/write.test.ts` | 新增 | WriteTool 测试 |
| `tests/read.test.ts` | 新增 | ReadTool 测试 |
| `tests/glob.test.ts` | 新增 | GlobTool 测试 |
| `tests/grep.test.ts` | 新增 | GrepTool 测试 |
| `tests/web-fetch.test.ts` | 新增 | WebFetchTool 测试 |

### 2.3 Interface / API Design

#### Tool Class（核心）

```ts
// src/tools/tool.ts
import { z } from 'zod';
import { zodToJsonSchema } from 'zod-to-json-schema';
import type { ToolDefinition } from '../model.js';

export class Tool<T extends z.ZodSchema> {
  readonly name: string;
  readonly description: string;
  readonly schema: T;
  private readonly runFn: (args: z.infer<T>) => Promise<string> | string;

  constructor(config: {
    name: string;
    description: string;
    schema: T;
    run: (args: z.infer<T>) => Promise<string> | string;
  }) {
    this.name = config.name;
    this.description = config.description;
    this.schema = config.schema;
    this.runFn = config.run;
  }

  get definition(): ToolDefinition {
    return {
      type: 'function',
      function: {
        name: this.name,
        description: this.description,
        parameters: zodToJsonSchema(this.schema, {
          name: this.name,
          $refStrategy: 'none',
        }) as Record<string, unknown>,
      },
    };
  }

  async execute(args: Record<string, unknown>): Promise<string> {
    try {
      const parsed = this.schema.parse(args);
      const result = await this.runFn(parsed);
      return result;
    } catch (error) {
      return error instanceof Error ? error.message : String(error);
    }
  }
}
```

#### WriteTool

```ts
import { z } from 'zod';
import { mkdir, writeFile } from 'node:fs/promises';
import { dirname } from 'node:path';
import { Tool } from './tool.js';

export const writeTool = new Tool({
  name: 'write',
  description: 'Write content to a file. Creates parent directories if needed.',
  schema: z.object({
    path: z.string().describe('File path'),
    content: z.string().describe('Content to write'),
  }),
  run: async ({ path, content }) => {
    await mkdir(dirname(path), { recursive: true });
    await writeFile(path, content, 'utf-8');
    return `File written: ${path}`;
  },
});
```

#### ReadTool

```ts
import { z } from 'zod';
import { readFile } from 'node:fs/promises';
import { Tool } from './tool.js';

export const readTool = new Tool({
  name: 'read',
  description: 'Read a text file. Supports line-based slicing with offset and limit.',
  schema: z.object({
    path: z.string().describe('File path'),
    offset: z.number().optional().describe('Start line (1-based)'),
    limit: z.number().optional().describe('Max lines to read'),
  }),
  run: async ({ path, offset, limit }) => {
    const content = await readFile(path, 'utf-8');
    const lines = content.split('\n');
    const start = offset ? Math.max(0, offset - 1) : 0;
    const end = limit ? start + limit : lines.length;
    return lines.slice(start, end).join('\n');
  },
});
```

#### GlobTool

```ts
import { z } from 'zod';
import { glob } from 'node:fs/promises';
import { Tool } from './tool.js';

export const globTool = new Tool({
  name: 'glob',
  description: 'Find files matching a glob pattern',
  schema: z.object({
    pattern: z.string().describe('Glob pattern'),
    cwd: z.string().optional().describe('Working directory'),
  }),
  run: async ({ pattern, cwd }) => {
    const results = await glob(pattern, { cwd });
    return results.join('\n') || '(no matches)';
  },
});
```

#### GrepTool

```ts
import { z } from 'zod';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
import { Tool } from './tool.js';

const execFileAsync = promisify(execFile);

export const grepTool = new Tool({
  name: 'grep',
  description: 'Search file contents using ripgrep (rg)',
  schema: z.object({
    pattern: z.string().describe('Regex pattern'),
    path: z.string().describe('File or directory to search'),
    glob: z.string().optional().describe('Glob filter for files'),
  }),
  run: async ({ pattern, path, glob: globFilter }) => {
    const rgArgs = ['-n', '-e', pattern];
    if (globFilter) rgArgs.push('--glob', globFilter);
    rgArgs.push(path);
    const { stdout } = await execFileAsync('rg', rgArgs);
    return stdout || '(no matches)';
  },
});
```

#### WebFetchTool

```ts
import { z } from 'zod';
import { Tool } from './tool.js';

export const webFetchTool = new Tool({
  name: 'web_fetch',
  description: 'Fetch content from a URL. Large responses are truncated.',
  schema: z.object({
    url: z.string().describe('URL to fetch'),
    maxLength: z.number().optional().describe('Max characters to return (default 200000)'),
  }),
  run: async ({ url, maxLength }) => {
    const res = await fetch(url);
    if (!res.ok) {
      throw new Error(`HTTP ${res.status} ${res.statusText}`);
    }
    let text = await res.text();
    const limit = maxLength ?? 200_000;
    if (text.length > limit) {
      text = text.slice(0, limit) + `\n... (truncated, ${text.length} total)`;
    }
    return text;
  },
});
```

#### AgentLoop 修改

```ts
// src/agent-loop.ts
import { Tool } from './tools/tool.js';  // 删除原有的 interface Tool

export interface AgentLoopConfig {
  model: Model;
  systemPrompt?: string;
  tools?: Tool[];  // 现在指向 concrete class
}
```

### 2.4 Implementation Steps

- [ ] 1. 安装依赖 `pnpm add zod zod-to-json-schema`
- [ ] 2. 创建 `src/tools/tool.ts`（concrete class）
- [ ] 3. 创建 `src/tools/write.ts`
- [ ] 4. 创建 `src/tools/read.ts`
- [ ] 5. 创建 `src/tools/glob.ts`
- [ ] 6. 创建 `src/tools/grep.ts`
- [ ] 7. 创建 `src/tools/web-fetch.ts`
- [ ] 8. 创建 `src/tools/index.ts` 统一导出
- [ ] 9. 修改 `src/agent-loop.ts`：删除 `interface Tool`，改为 import Tool class
- [ ] 10. 修改 `src/index.ts`：追加 tool 导出
- [ ] 11. 写基类测试 `tests/tool.test.ts`
- [ ] 12. 写各 tool 的单元测试
- [ ] 13. 运行 `pnpm run test` 确保通过
- [ ] 14. 运行 `pnpm run lint` 修复格式

### 2.5 Testing Strategy

| 测试文件 | 覆盖点 |
|---|---|
| `tests/tool.test.ts` | `definition` 正确转换 zod schema 为 JSON Schema；`execute` 正确解析参数；zod 校验失败返回错误信息；`execute` 捕获异常 |
| `tests/write.test.ts` | 正常写入、自动创建父目录、覆盖已有文件 |
| `tests/read.test.ts` | 完整读取、`offset`/`limit` 切片、1-based 行号 |
| `tests/glob.test.ts` | 匹配当前目录文件、支持 `cwd` |
| `tests/grep.test.ts` | Mock `execFile` 验证 rg 参数组装；无匹配时返回提示 |
| `tests/web-fetch.test.ts` | Mock `fetch` 验证请求；大内容截断；HTTP 错误处理 |

**Mock 策略**：
- `fetch`：使用 `vi.fn()` mock global fetch
- `execFile`：mock `node:child_process` 的 `execFile`，避免依赖系统 rg
- 文件系统：直接使用真实 `fs`，在临时目录操作，测试后清理

### 2.6 Risks & Mitigations

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 系统未安装 `rg` | GrepTool 执行失败 | 错误会被 `execute` 捕获并返回错误字符串，不会 crash AgentLoop |
| 文件路径越界 | Write/Read 可能访问敏感路径 | MVP 阶段不处理；后续可加 `cwd` 白名单或路径归一化 |
| `fetch` 无超时 | WebFetch 可能挂起 | MVP 阶段依赖底层 `fetch` 默认行为；后续可加 `AbortController` |
| `fs.glob` 兼容性 | Node <22 不支持 | 项目 `@types/node` 为 24.x，假设运行环境匹配 |
| `zod-to-json-schema` 转换不完美 | 某些复杂 zod 类型生成的 JSON Schema 不准确 | MVP 阶段只用基础类型（string, number, object, optional），不会触发边缘 case |
